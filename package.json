{
  "name": "hogar-maradona",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tinacms dev -c \"astro dev\"",
    "build": "if [ -n \"$TINA_PUBLIC_CLIENT_ID\" ]; then tinacms build; fi && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.1",
    "@tinacms/cli": "^2.5.6",
    "astro": "^5.12.0",
    "tinacms": "^3.11.0"
  }
}
